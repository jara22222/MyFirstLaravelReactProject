{
    "buildCommand": "npm run build",
    "outputDirectory": "public",
    "env": {
        "NODE_ENV": "production"
    },
    "installCommand": "npm install",
    "framework": null,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/public/$1"
        }
    ]
}
